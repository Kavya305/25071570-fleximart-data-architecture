# Part 1 – Database Design and ETL Pipeline

This part focuses on designing a relational database for the FlexiMart e-commerce
system and implementing an ETL pipeline to load and clean raw transactional data.

The objective is to extract data from CSV files, transform it to ensure data
quality and consistency, and load it into a normalized MySQL database.
